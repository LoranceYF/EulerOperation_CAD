可执行文件目录：
\CAD_Proj\_output\Win32\Debug\bin

在openGL窗口中，鼠标左键拖动可以旋转视角，鼠标右键拖动可以改变缩放比例。

对于欧拉操作的使用：
由于能力和时间有限，因此并没有实现交互式建模。

但是由于欧拉函数均采用模型元素Id作为输入，结合show()函数就已经足够完成交互式建模的前提要求。

Example.cpp则给出了几个模型构造示例，采用的均为欧拉操作序列。

如果需要单独建模，参考Example.cpp中的模型构造函数的形式，另外构造一个新的模型函数，并在main函数中调用即可。